# 🏷️ Interactive Name Tag Generator

A simple web application built to practice **DOM Manipulation** and **Event Listeners** in JavaScript.

### 🚀 Features
- **Dynamic Updates**: Real-time name display using `.textContent`.
- **Input Validation**: Prevents empty names using basic logic.
- **Responsive Design**: Styled with CSS Flexbox for a clean UI.

### 🛠️ Tech Stack
- HTML5
- CSS3 (Flexbox)
- JavaScript (Vanilla)

### 🎓 What I Learned
- How to select DOM elements using `getElementById`.
- The difference between `.value` (inputs) and `.textContent` (containers).
- Handling "Click" events to trigger UI changes.
